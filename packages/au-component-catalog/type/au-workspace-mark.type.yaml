# Owned by component-contract. The INTERFACE for `<au-workspace-mark>` — the workspace initial tile.
#
# The type NAME is the custom-element tag. `name` is the only prop (data-in); the mark renders its
# first glyph in a monochrome surface tile (surface-2 + inset hairline, NEVER a coloured fill) and
# labels itself for a11y from `name`. Set-INDEPENDENT: a consumer types against THIS.
#: The interface for <au-workspace-mark>: the workspace initial in a monochrome rounded tile. `name`'s
#: first glyph is the mark; it self-labels for a11y.
extends: ui-component::component-contract
fields:
  #: The workspace name — its first glyph becomes the mark and its a11y label.
  name?: String
meta:
  - type: ui-component-meta::component-contract
    preview: '<au-workspace-mark name="my-workspace"></au-workspace-mark> <au-workspace-mark name="Engine"></au-workspace-mark> <au-workspace-mark name="Docs"></au-workspace-mark>'
