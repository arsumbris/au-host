// Generated by the component-layer React wrapper codegen (`pnpm gen:react`). Do not edit by hand.
//
// The `./react` sub-output. One `<AuXxx>` React component per `<au-*>` tag, via `makeAuWrapper`
// (react-runtime): it renders the TAG STRING, sets declared props as element PROPERTIES, and wires
// declared events as typed `onAuXxx` props. It imports the NEUTRAL interfaces from ./generated and the
// runtime — NEVER a component-set class, so a host-swapped set still wins the tag (live boot-swap).
//
// Also re-exports the JSX augmentation (./react-jsx) so residual intrinsic `<au-*>` usage + children
// still type-check. A React consumer imports the wrappers by name:
//   import { AuAccordion } from '@arsumbris/au-component-catalog/react'
import './react-jsx.ts'
import { makeAuWrapper, type AuHostProps, type AuEventHandler } from './react-runtime.ts'
import type { AuAccordion as AuAccordionShape, AuAccordionItem as AuAccordionItemShape, AuBadge as AuBadgeShape, AuBanner as AuBannerShape, AuBreadcrumb as AuBreadcrumbShape, AuButton as AuButtonShape, AuCard as AuCardShape, AuCheckbox as AuCheckboxShape, AuChevron as AuChevronShape, AuChip as AuChipShape, AuChordInput as AuChordInputShape, AuCloseButton as AuCloseButtonShape, AuCodeBlock as AuCodeBlockShape, AuCodeSample as AuCodeSampleShape, AuColorPicker as AuColorPickerShape, AuCombobox as AuComboboxShape, AuCommandPalette as AuCommandPaletteShape, AuDetailList as AuDetailListShape, AuDiffLine as AuDiffLineShape, AuDivider as AuDividerShape, AuDrawer as AuDrawerShape, AuDropZone as AuDropZoneShape, AuEmptyState as AuEmptyStateShape, AuField as AuFieldShape, AuGripGlyph as AuGripGlyphShape, AuHovercard as AuHovercardShape, AuIcon as AuIconShape, AuIconButton as AuIconButtonShape, AuInput as AuInputShape, AuKbd as AuKbdShape, AuListRow as AuListRowShape, AuMenu as AuMenuShape, AuMenuItem as AuMenuItemShape, AuMeter as AuMeterShape, AuModal as AuModalShape, AuNavGroup as AuNavGroupShape, AuNavItem as AuNavItemShape, AuNumberInput as AuNumberInputShape, AuOutputLog as AuOutputLogShape, AuPagination as AuPaginationShape, AuPaneFrame as AuPaneFrameShape, AuPaneHeader as AuPaneHeaderShape, AuPaneTarget as AuPaneTargetShape, AuPill as AuPillShape, AuPopover as AuPopoverShape, AuRadioGroup as AuRadioGroupShape, AuScrollArea as AuScrollAreaShape, AuSectionHeader as AuSectionHeaderShape, AuSegmentedControl as AuSegmentedControlShape, AuSelect as AuSelectShape, AuSettingsSection as AuSettingsSectionShape, AuSkeleton as AuSkeletonShape, AuSlider as AuSliderShape, AuSpinner as AuSpinnerShape, AuSplitter as AuSplitterShape, AuStatusDot as AuStatusDotShape, AuStepper as AuStepperShape, AuSwatch as AuSwatchShape, AuSwitch as AuSwitchShape, AuTab as AuTabShape, AuTabBar as AuTabBarShape, AuTabPanel as AuTabPanelShape, AuTabStripCell as AuTabStripCellShape, AuTable as AuTableShape, AuTableBody as AuTableBodyShape, AuTableCell as AuTableCellShape, AuTableHead as AuTableHeadShape, AuTableHeaderCell as AuTableHeaderCellShape, AuTableRow as AuTableRowShape, AuTabs as AuTabsShape, AuTag as AuTagShape, AuTextarea as AuTextareaShape, AuToast as AuToastShape, AuToggleChip as AuToggleChipShape, AuToolbar as AuToolbarShape, AuToolbarGroup as AuToolbarGroupShape, AuToolbarSpacer as AuToolbarSpacerShape, AuTooltip as AuTooltipShape, AuTree as AuTreeShape, AuTreeItem as AuTreeItemShape, AuTreeRow as AuTreeRowShape, AuTypedValueEditor as AuTypedValueEditorShape, AuViewerSwitch as AuViewerSwitchShape, AuWorkspaceMark as AuWorkspaceMarkShape, AuWorkspaceSwitcher as AuWorkspaceSwitcherShape } from './generated.ts'

export type { AuHostProps, AuEventHandler } from './react-runtime.ts'

/** React wrapper for `<au-accordion>` — renders the tag; props → element properties; events → listeners. */
export type AuAccordionProps = AuHostProps & Omit<AuAccordionShape, 'type'> & {
  onAuChange?: AuEventHandler
}
export const AuAccordion = makeAuWrapper<AuAccordionProps>('au-accordion', ['mode', 'variant', 'density', 'collapsible'], { onAuChange: 'au-change' })

/** React wrapper for `<au-accordion-item>` — renders the tag; props → element properties; events → listeners. */
export type AuAccordionItemProps = AuHostProps & Omit<AuAccordionItemShape, 'type'> & {
  onAuToggle?: AuEventHandler
}
export const AuAccordionItem = makeAuWrapper<AuAccordionItemProps>('au-accordion-item', ['value', 'label', 'icon', 'sublabel', 'disabled', 'selected', 'open', 'variant', 'density'], { onAuToggle: 'au-toggle' })

/** React wrapper for `<au-badge>` — renders the tag; props → element properties; events → listeners. */
export type AuBadgeProps = AuHostProps & Omit<AuBadgeShape, 'type'>
export const AuBadge = makeAuWrapper<AuBadgeProps>('au-badge', ['variant', 'tone'], {})

/** React wrapper for `<au-banner>` — renders the tag; props → element properties; events → listeners. */
export type AuBannerProps = AuHostProps & Omit<AuBannerShape, 'type'> & {
  onAuDismiss?: AuEventHandler
}
export const AuBanner = makeAuWrapper<AuBannerProps>('au-banner', ['tone', 'variant', 'placement', 'emphasis', 'heading', 'message', 'icon', 'dismissible'], { onAuDismiss: 'au-dismiss' })

/** React wrapper for `<au-breadcrumb>` — renders the tag; props → element properties; events → listeners. */
export type AuBreadcrumbProps = AuHostProps & Omit<AuBreadcrumbShape, 'type'> & {
  onAuNavigate?: AuEventHandler
}
export const AuBreadcrumb = makeAuWrapper<AuBreadcrumbProps>('au-breadcrumb', ['items', 'separator', 'maxItems', 'label'], { onAuNavigate: 'au-navigate' })

/** React wrapper for `<au-button>` — renders the tag; props → element properties; events → listeners. */
export type AuButtonProps = AuHostProps & Omit<AuButtonShape, 'type'> & {
  onAuActivate?: AuEventHandler
}
export const AuButton = makeAuWrapper<AuButtonProps>('au-button', ['variant', 'size', 'disabled', 'loading', 'icon'], { onAuActivate: 'au-activate' })

/** React wrapper for `<au-card>` — renders the tag; props → element properties; events → listeners. */
export type AuCardProps = AuHostProps & Omit<AuCardShape, 'type'>
export const AuCard = makeAuWrapper<AuCardProps>('au-card', ['variant', 'interactive', 'roomy'], {})

/** React wrapper for `<au-checkbox>` — renders the tag; props → element properties; events → listeners. */
export type AuCheckboxProps = AuHostProps & Omit<AuCheckboxShape, 'type'> & {
  onAuChange?: AuEventHandler
}
export const AuCheckbox = makeAuWrapper<AuCheckboxProps>('au-checkbox', ['checked', 'indeterminate', 'disabled', 'size', 'label'], { onAuChange: 'au-change' })

/** React wrapper for `<au-chevron>` — renders the tag; props → element properties; events → listeners. */
export type AuChevronProps = AuHostProps & Omit<AuChevronShape, 'type'> & {
  onAuActivate?: AuEventHandler
}
export const AuChevron = makeAuWrapper<AuChevronProps>('au-chevron', ['open', 'label', 'disabled'], { onAuActivate: 'au-activate' })

/** React wrapper for `<au-chip>` — renders the tag; props → element properties; events → listeners. */
export type AuChipProps = AuHostProps & Omit<AuChipShape, 'type'> & {
  onAuRemove?: AuEventHandler
}
export const AuChip = makeAuWrapper<AuChipProps>('au-chip', ['label', 'variant', 'removable'], { onAuRemove: 'au-remove' })

/** React wrapper for `<au-chord-input>` — renders the tag; props → element properties; events → listeners. */
export type AuChordInputProps = AuHostProps & Omit<AuChordInputShape, 'type'> & {
  onAuChordChange?: AuEventHandler
}
export const AuChordInput = makeAuWrapper<AuChordInputProps>('au-chord-input', ['chord', 'placeholder', 'disabled'], { onAuChordChange: 'au-chord-change' })

/** React wrapper for `<au-close-button>` — renders the tag; props → element properties; events → listeners. */
export type AuCloseButtonProps = AuHostProps & Omit<AuCloseButtonShape, 'type'> & {
  onAuActivate?: AuEventHandler
}
export const AuCloseButton = makeAuWrapper<AuCloseButtonProps>('au-close-button', ['tone', 'label', 'disabled'], { onAuActivate: 'au-activate' })

/** React wrapper for `<au-code-block>` — renders the tag; props → element properties; events → listeners. */
export type AuCodeBlockProps = AuHostProps & Omit<AuCodeBlockShape, 'type'>
export const AuCodeBlock = makeAuWrapper<AuCodeBlockProps>('au-code-block', ['code', 'language', 'copy', 'copyText'], {})

/** React wrapper for `<au-code-sample>` — renders the tag; props → element properties; events → listeners. */
export type AuCodeSampleProps = AuHostProps & Omit<AuCodeSampleShape, 'type'>
export const AuCodeSample = makeAuWrapper<AuCodeSampleProps>('au-code-sample', ['activeLine'], {})

/** React wrapper for `<au-color-picker>` — renders the tag; props → element properties; events → listeners. */
export type AuColorPickerProps = AuHostProps & Omit<AuColorPickerShape, 'type'> & {
  onAuChange?: AuEventHandler
}
export const AuColorPicker = makeAuWrapper<AuColorPickerProps>('au-color-picker', ['value', 'alpha', 'size', 'label'], { onAuChange: 'au-change' })

/** React wrapper for `<au-combobox>` — renders the tag; props → element properties; events → listeners. */
export type AuComboboxProps = AuHostProps & Omit<AuComboboxShape, 'type'> & {
  onAuChange?: AuEventHandler
  onAuInput?: AuEventHandler
  onAuCreate?: AuEventHandler
}
export const AuCombobox = makeAuWrapper<AuComboboxProps>('au-combobox', ['options', 'value', 'values', 'multiple', 'mode', 'placeholder', 'searchPlaceholder', 'filterable', 'remote', 'loading', 'creatable', 'disabled', 'label', 'open', 'query'], { onAuChange: 'au-change', onAuInput: 'au-input', onAuCreate: 'au-create' })

/** React wrapper for `<au-command-palette>` — renders the tag; props → element properties; events → listeners. */
export type AuCommandPaletteProps = AuHostProps & Omit<AuCommandPaletteShape, 'type'> & {
  onAuRun?: AuEventHandler
}
export const AuCommandPalette = makeAuWrapper<AuCommandPaletteProps>('au-command-palette', ['items', 'variant', 'placeholder', 'loading'], { onAuRun: 'au-run' })

/** React wrapper for `<au-detail-list>` — renders the tag; props → element properties; events → listeners. */
export type AuDetailListProps = AuHostProps & Omit<AuDetailListShape, 'type'>
export const AuDetailList = makeAuWrapper<AuDetailListProps>('au-detail-list', ['items', 'layout', 'separators'], {})

/** React wrapper for `<au-diff-line>` — renders the tag; props → element properties; events → listeners. */
export type AuDiffLineProps = AuHostProps & Omit<AuDiffLineShape, 'type'>
export const AuDiffLine = makeAuWrapper<AuDiffLineProps>('au-diff-line', ['kind', 'oldNo', 'newNo'], {})

/** React wrapper for `<au-divider>` — renders the tag; props → element properties; events → listeners. */
export type AuDividerProps = AuHostProps & Omit<AuDividerShape, 'type'>
export const AuDivider = makeAuWrapper<AuDividerProps>('au-divider', ['orientation', 'weight', 'inset'], {})

/** React wrapper for `<au-drawer>` — renders the tag; props → element properties; events → listeners. */
export type AuDrawerProps = AuHostProps & Omit<AuDrawerShape, 'type'> & {
  onAuClose?: AuEventHandler
}
export const AuDrawer = makeAuWrapper<AuDrawerProps>('au-drawer', ['open', 'side', 'size', 'posture', 'heading', 'subtitle', 'dismissible', 'resizable'], { onAuClose: 'au-close' })

/** React wrapper for `<au-drop-zone>` — renders the tag; props → element properties; events → listeners. */
export type AuDropZoneProps = AuHostProps & Omit<AuDropZoneShape, 'type'>
export const AuDropZone = makeAuWrapper<AuDropZoneProps>('au-drop-zone', ['zone', 'active', 'label', 'edgeRatio', 'radius'], {})

/** React wrapper for `<au-empty-state>` — renders the tag; props → element properties; events → listeners. */
export type AuEmptyStateProps = AuHostProps & Omit<AuEmptyStateShape, 'type'>
export const AuEmptyState = makeAuWrapper<AuEmptyStateProps>('au-empty-state', ['label', 'hint'], {})

/** React wrapper for `<au-field>` — renders the tag; props → element properties; events → listeners. */
export type AuFieldProps = AuHostProps & Omit<AuFieldShape, 'type'>
export const AuField = makeAuWrapper<AuFieldProps>('au-field', ['label', 'hint', 'error', 'required', 'layout'], {})

/** React wrapper for `<au-grip-glyph>` — renders the tag; props → element properties; events → listeners. */
export type AuGripGlyphProps = AuHostProps & Omit<AuGripGlyphShape, 'type'>
export const AuGripGlyph = makeAuWrapper<AuGripGlyphProps>('au-grip-glyph', ['dragging', 'disabled', 'label'], {})

/** React wrapper for `<au-hovercard>` — renders the tag; props → element properties; events → listeners. */
export type AuHovercardProps = AuHostProps & Omit<AuHovercardShape, 'type'>
export const AuHovercard = makeAuWrapper<AuHovercardProps>('au-hovercard', ['origin'], {})

/** React wrapper for `<au-icon>` — renders the tag; props → element properties; events → listeners. */
export type AuIconProps = AuHostProps & Omit<AuIconShape, 'type'>
export const AuIcon = makeAuWrapper<AuIconProps>('au-icon', ['name', 'size', 'label'], {})

/** React wrapper for `<au-icon-button>` — renders the tag; props → element properties; events → listeners. */
export type AuIconButtonProps = AuHostProps & Omit<AuIconButtonShape, 'type'> & {
  onAuActivate?: AuEventHandler
}
export const AuIconButton = makeAuWrapper<AuIconButtonProps>('au-icon-button', ['label', 'size', 'surface', 'disabled'], { onAuActivate: 'au-activate' })

/** React wrapper for `<au-input>` — renders the tag; props → element properties; events → listeners. */
export type AuInputProps = AuHostProps & Omit<AuInputShape, 'type'> & {
  onAuInput?: AuEventHandler
  onAuChange?: AuEventHandler
}
export const AuInput = makeAuWrapper<AuInputProps>('au-input', ['value', 'placeholder', 'inputType', 'size', 'disabled', 'error', 'borderless', 'spellcheck'], { onAuInput: 'au-input', onAuChange: 'au-change' })

/** React wrapper for `<au-kbd>` — renders the tag; props → element properties; events → listeners. */
export type AuKbdProps = AuHostProps & Omit<AuKbdShape, 'type'>
export const AuKbd = makeAuWrapper<AuKbdProps>('au-kbd', ['keys', 'mac'], {})

/** React wrapper for `<au-list-row>` — renders the tag; props → element properties; events → listeners. */
export type AuListRowProps = AuHostProps & Omit<AuListRowShape, 'type'>
export const AuListRow = makeAuWrapper<AuListRowProps>('au-list-row', ['wrap', 'primary', 'secondary', 'interactive', 'selected', 'disabled'], {})

/** React wrapper for `<au-menu>` — renders the tag; props → element properties; events → listeners. */
export type AuMenuProps = AuHostProps & Omit<AuMenuShape, 'type'>
export const AuMenu = makeAuWrapper<AuMenuProps>('au-menu', [], {})

/** React wrapper for `<au-menu-item>` — renders the tag; props → element properties; events → listeners. */
export type AuMenuItemProps = AuHostProps & Omit<AuMenuItemShape, 'type'> & {
  onAuActivate?: AuEventHandler
}
export const AuMenuItem = makeAuWrapper<AuMenuItemProps>('au-menu-item', ['label', 'description', 'icon', 'shortcut', 'active', 'disabled', 'destructive', 'submenu'], { onAuActivate: 'au-activate' })

/** React wrapper for `<au-meter>` — renders the tag; props → element properties; events → listeners. */
export type AuMeterProps = AuHostProps & Omit<AuMeterShape, 'type'>
export const AuMeter = makeAuWrapper<AuMeterProps>('au-meter', ['value', 'label', 'valueLabel', 'tone', 'indeterminate'], {})

/** React wrapper for `<au-modal>` — renders the tag; props → element properties; events → listeners. */
export type AuModalProps = AuHostProps & Omit<AuModalShape, 'type'> & {
  onAuClose?: AuEventHandler
}
export const AuModal = makeAuWrapper<AuModalProps>('au-modal', ['heading', 'variant', 'dismissible'], { onAuClose: 'au-close' })

/** React wrapper for `<au-nav-group>` — renders the tag; props → element properties; events → listeners. */
export type AuNavGroupProps = AuHostProps & Omit<AuNavGroupShape, 'type'> & {
  onAuToggle?: AuEventHandler
}
export const AuNavGroup = makeAuWrapper<AuNavGroupProps>('au-nav-group', ['label', 'count', 'collapsible', 'open', 'sans', 'caps', 'chevronEnd'], { onAuToggle: 'au-toggle' })

/** React wrapper for `<au-nav-item>` — renders the tag; props → element properties; events → listeners. */
export type AuNavItemProps = AuHostProps & Omit<AuNavItemShape, 'type'> & {
  onAuActivate?: AuEventHandler
}
export const AuNavItem = makeAuWrapper<AuNavItemProps>('au-nav-item', ['selected', 'disabled'], { onAuActivate: 'au-activate' })

/** React wrapper for `<au-number-input>` — renders the tag; props → element properties; events → listeners. */
export type AuNumberInputProps = AuHostProps & Omit<AuNumberInputShape, 'type'> & {
  onAuInput?: AuEventHandler
  onAuChange?: AuEventHandler
}
export const AuNumberInput = makeAuWrapper<AuNumberInputProps>('au-number-input', ['value', 'min', 'max', 'step', 'largeStep', 'fineStep', 'precision', 'wrap', 'unit', 'leading', 'variant', 'align', 'size', 'scrubbable', 'loading', 'error', 'placeholder', 'disabled', 'readonly', 'label'], { onAuInput: 'au-input', onAuChange: 'au-change' })

/** React wrapper for `<au-output-log>` — renders the tag; props → element properties; events → listeners. */
export type AuOutputLogProps = AuHostProps & Omit<AuOutputLogShape, 'type'>
export const AuOutputLog = makeAuWrapper<AuOutputLogProps>('au-output-log', ['lines', 'maxLines'], {})

/** React wrapper for `<au-pagination>` — renders the tag; props → element properties; events → listeners. */
export type AuPaginationProps = AuHostProps & Omit<AuPaginationShape, 'type'> & {
  onAuPageChange?: AuEventHandler
  onAuPageSizeChange?: AuEventHandler
}
export const AuPagination = makeAuWrapper<AuPaginationProps>('au-pagination', ['page', 'pageCount', 'variant', 'size', 'total', 'summary', 'pageSize', 'pageSizeOptions', 'siblingCount', 'boundaryCount', 'showFirstLast', 'loading', 'label'], { onAuPageChange: 'au-page-change', onAuPageSizeChange: 'au-page-size-change' })

/** React wrapper for `<au-pane-frame>` — renders the tag; props → element properties; events → listeners. */
export type AuPaneFrameProps = AuHostProps & Omit<AuPaneFrameShape, 'type'> & {
  onAuLabelDblclick?: AuEventHandler
}
export const AuPaneFrame = makeAuWrapper<AuPaneFrameProps>('au-pane-frame', ['focused', 'error', 'preview', 'flush', 'nested', 'engaged'], { onAuLabelDblclick: 'au-label-dblclick' })

/** React wrapper for `<au-pane-header>` — renders the tag; props → element properties; events → listeners. */
export type AuPaneHeaderProps = AuHostProps & Omit<AuPaneHeaderShape, 'type'>
export const AuPaneHeader = makeAuWrapper<AuPaneHeaderProps>('au-pane-header', ['compact', 'context', 'surface', 'focused', 'divider', 'draggable'], {})

/** React wrapper for `<au-pane-target>` — renders the tag; props → element properties; events → listeners. */
export type AuPaneTargetProps = AuHostProps & Omit<AuPaneTargetShape, 'type'> & {
  onAuActivate?: AuEventHandler
}
export const AuPaneTarget = makeAuWrapper<AuPaneTargetProps>('au-pane-target', ['active', 'hint', 'label', 'disabled'], { onAuActivate: 'au-activate' })

/** React wrapper for `<au-pill>` — renders the tag; props → element properties; events → listeners. */
export type AuPillProps = AuHostProps & Omit<AuPillShape, 'type'>
export const AuPill = makeAuWrapper<AuPillProps>('au-pill', ['dot', 'draft', 'muted', 'tone', 'pulse'], {})

/** React wrapper for `<au-popover>` — renders the tag; props → element properties; events → listeners. */
export type AuPopoverProps = AuHostProps & Omit<AuPopoverShape, 'type'>
export const AuPopover = makeAuWrapper<AuPopoverProps>('au-popover', ['side', 'arrow', 'heading', 'scrollable'], {})

/** React wrapper for `<au-radio-group>` — renders the tag; props → element properties; events → listeners. */
export type AuRadioGroupProps = AuHostProps & Omit<AuRadioGroupShape, 'type'> & {
  onAuChange?: AuEventHandler
}
export const AuRadioGroup = makeAuWrapper<AuRadioGroupProps>('au-radio-group', ['items', 'value', 'disabled', 'label'], { onAuChange: 'au-change' })

/** React wrapper for `<au-scroll-area>` — renders the tag; props → element properties; events → listeners. */
export type AuScrollAreaProps = AuHostProps & Omit<AuScrollAreaShape, 'type'>
export const AuScrollArea = makeAuWrapper<AuScrollAreaProps>('au-scroll-area', ['content', 'axis'], {})

/** React wrapper for `<au-section-header>` — renders the tag; props → element properties; events → listeners. */
export type AuSectionHeaderProps = AuHostProps & Omit<AuSectionHeaderShape, 'type'> & {
  onAuToggle?: AuEventHandler
}
export const AuSectionHeader = makeAuWrapper<AuSectionHeaderProps>('au-section-header', ['count', 'collapsible', 'open', 'sans', 'caps', 'chevronEnd', 'locked'], { onAuToggle: 'au-toggle' })

/** React wrapper for `<au-segmented-control>` — renders the tag; props → element properties; events → listeners. */
export type AuSegmentedControlProps = AuHostProps & Omit<AuSegmentedControlShape, 'type'> & {
  onAuChange?: AuEventHandler
}
export const AuSegmentedControl = makeAuWrapper<AuSegmentedControlProps>('au-segmented-control', ['items', 'value', 'disabled', 'label'], { onAuChange: 'au-change' })

/** React wrapper for `<au-select>` — renders the tag; props → element properties; events → listeners. */
export type AuSelectProps = AuHostProps & Omit<AuSelectShape, 'type'> & {
  onAuChange?: AuEventHandler
}
export const AuSelect = makeAuWrapper<AuSelectProps>('au-select', ['options', 'value', 'placeholder', 'disabled', 'size', 'label', 'triggerIcon'], { onAuChange: 'au-change' })

/** React wrapper for `<au-settings-section>` — renders the tag; props → element properties; events → listeners. */
export type AuSettingsSectionProps = AuHostProps & Omit<AuSettingsSectionShape, 'type'>
export const AuSettingsSection = makeAuWrapper<AuSettingsSectionProps>('au-settings-section', ['heading', 'description', 'compact'], {})

/** React wrapper for `<au-skeleton>` — renders the tag; props → element properties; events → listeners. */
export type AuSkeletonProps = AuHostProps & Omit<AuSkeletonShape, 'type'>
export const AuSkeleton = makeAuWrapper<AuSkeletonProps>('au-skeleton', ['variant', 'width', 'height', 'lines', 'radius', 'label'], {})

/** React wrapper for `<au-slider>` — renders the tag; props → element properties; events → listeners. */
export type AuSliderProps = AuHostProps & Omit<AuSliderShape, 'type'> & {
  onAuInput?: AuEventHandler
  onAuChange?: AuEventHandler
}
export const AuSlider = makeAuWrapper<AuSliderProps>('au-slider', ['value', 'min', 'max', 'step', 'disabled', 'label'], { onAuInput: 'au-input', onAuChange: 'au-change' })

/** React wrapper for `<au-spinner>` — renders the tag; props → element properties; events → listeners. */
export type AuSpinnerProps = AuHostProps & Omit<AuSpinnerShape, 'type'>
export const AuSpinner = makeAuWrapper<AuSpinnerProps>('au-spinner', ['size', 'label'], {})

/** React wrapper for `<au-splitter>` — renders the tag; props → element properties; events → listeners. */
export type AuSplitterProps = AuHostProps & Omit<AuSplitterShape, 'type'>
export const AuSplitter = makeAuWrapper<AuSplitterProps>('au-splitter', ['orientation', 'dragging', 'disabled', 'knob', 'line', 'knobSize'], {})

/** React wrapper for `<au-status-dot>` — renders the tag; props → element properties; events → listeners. */
export type AuStatusDotProps = AuHostProps & Omit<AuStatusDotShape, 'type'>
export const AuStatusDot = makeAuWrapper<AuStatusDotProps>('au-status-dot', ['tone', 'variant', 'size', 'glow', 'pulse', 'label'], {})

/** React wrapper for `<au-stepper>` — renders the tag; props → element properties; events → listeners. */
export type AuStepperProps = AuHostProps & Omit<AuStepperShape, 'type'> & {
  onAuStepSelect?: AuEventHandler
}
export const AuStepper = makeAuWrapper<AuStepperProps>('au-stepper', ['steps', 'current', 'variant', 'numbered', 'separator', 'interactive', 'countLabel', 'label'], { onAuStepSelect: 'au-step-select' })

/** React wrapper for `<au-swatch>` — renders the tag; props → element properties; events → listeners. */
export type AuSwatchProps = AuHostProps & Omit<AuSwatchShape, 'type'>
export const AuSwatch = makeAuWrapper<AuSwatchProps>('au-swatch', ['value', 'size', 'alpha0', 'label'], {})

/** React wrapper for `<au-switch>` — renders the tag; props → element properties; events → listeners. */
export type AuSwitchProps = AuHostProps & Omit<AuSwitchShape, 'type'> & {
  onAuChange?: AuEventHandler
}
export const AuSwitch = makeAuWrapper<AuSwitchProps>('au-switch', ['checked', 'disabled', 'label'], { onAuChange: 'au-change' })

/** React wrapper for `<au-tab>` — renders the tag; props → element properties; events → listeners. */
export type AuTabProps = AuHostProps & Omit<AuTabShape, 'type'>
export const AuTab = makeAuWrapper<AuTabProps>('au-tab', ['value', 'disabled'], {})

/** React wrapper for `<au-tab-bar>` — renders the tag; props → element properties; events → listeners. */
export type AuTabBarProps = AuHostProps & Omit<AuTabBarShape, 'type'> & {
  onAuSelect?: AuEventHandler
  onAuClose?: AuEventHandler
  onAuAdd?: AuEventHandler
}
export const AuTabBar = makeAuWrapper<AuTabBarProps>('au-tab-bar', ['tabs', 'activeId', 'draggingId', 'add', 'closable'], { onAuSelect: 'au-select', onAuClose: 'au-close', onAuAdd: 'au-add' })

/** React wrapper for `<au-tab-panel>` — renders the tag; props → element properties; events → listeners. */
export type AuTabPanelProps = AuHostProps & Omit<AuTabPanelShape, 'type'>
export const AuTabPanel = makeAuWrapper<AuTabPanelProps>('au-tab-panel', ['value'], {})

/** React wrapper for `<au-tab-strip-cell>` — renders the tag; props → element properties; events → listeners. */
export type AuTabStripCellProps = AuHostProps & Omit<AuTabStripCellShape, 'type'> & {
  onAuActivate?: AuEventHandler
  onAuClose?: AuEventHandler
  onAuTabDragStart?: AuEventHandler
}
export const AuTabStripCell = makeAuWrapper<AuTabStripCellProps>('au-tab-strip-cell', ['icon', 'group', 'label', 'active', 'dirty', 'pinned', 'dragging', 'locked', 'preview', 'moveLocked', 'removeLocked', 'grip'], { onAuActivate: 'au-activate', onAuClose: 'au-close', onAuTabDragStart: 'au-tab-drag-start' })

/** React wrapper for `<au-table>` — renders the tag; props → element properties; events → listeners. */
export type AuTableProps = AuHostProps & Omit<AuTableShape, 'type'>
export const AuTable = makeAuWrapper<AuTableProps>('au-table', ['dense'], {})

/** React wrapper for `<au-table-body>` — renders the tag; props → element properties; events → listeners. */
export type AuTableBodyProps = AuHostProps & Omit<AuTableBodyShape, 'type'>
export const AuTableBody = makeAuWrapper<AuTableBodyProps>('au-table-body', [], {})

/** React wrapper for `<au-table-cell>` — renders the tag; props → element properties; events → listeners. */
export type AuTableCellProps = AuHostProps & Omit<AuTableCellShape, 'type'>
export const AuTableCell = makeAuWrapper<AuTableCellProps>('au-table-cell', ['align', 'muted'], {})

/** React wrapper for `<au-table-head>` — renders the tag; props → element properties; events → listeners. */
export type AuTableHeadProps = AuHostProps & Omit<AuTableHeadShape, 'type'>
export const AuTableHead = makeAuWrapper<AuTableHeadProps>('au-table-head', [], {})

/** React wrapper for `<au-table-header-cell>` — renders the tag; props → element properties; events → listeners. */
export type AuTableHeaderCellProps = AuHostProps & Omit<AuTableHeaderCellShape, 'type'> & {
  onAuSort?: AuEventHandler
}
export const AuTableHeaderCell = makeAuWrapper<AuTableHeaderCellProps>('au-table-header-cell', ['align', 'sortable', 'sortDir'], { onAuSort: 'au-sort' })

/** React wrapper for `<au-table-row>` — renders the tag; props → element properties; events → listeners. */
export type AuTableRowProps = AuHostProps & Omit<AuTableRowShape, 'type'> & {
  onAuSelect?: AuEventHandler
}
export const AuTableRow = makeAuWrapper<AuTableRowProps>('au-table-row', ['selectable', 'selected'], { onAuSelect: 'au-select' })

/** React wrapper for `<au-tabs>` — renders the tag; props → element properties; events → listeners. */
export type AuTabsProps = AuHostProps & Omit<AuTabsShape, 'type'> & {
  onAuTabChange?: AuEventHandler
}
export const AuTabs = makeAuWrapper<AuTabsProps>('au-tabs', ['variant', 'orientation', 'activationMode', 'mount', 'overflow', 'value', 'label'], { onAuTabChange: 'au-tab-change' })

/** React wrapper for `<au-tag>` — renders the tag; props → element properties; events → listeners. */
export type AuTagProps = AuHostProps & Omit<AuTagShape, 'type'>
export const AuTag = makeAuWrapper<AuTagProps>('au-tag', ['muted'], {})

/** React wrapper for `<au-textarea>` — renders the tag; props → element properties; events → listeners. */
export type AuTextareaProps = AuHostProps & Omit<AuTextareaShape, 'type'> & {
  onAuInput?: AuEventHandler
  onAuChange?: AuEventHandler
}
export const AuTextarea = makeAuWrapper<AuTextareaProps>('au-textarea', ['value', 'placeholder', 'rows', 'disabled', 'error', 'autoGrow', 'mono', 'size', 'spellcheck'], { onAuInput: 'au-input', onAuChange: 'au-change' })

/** React wrapper for `<au-toast>` — renders the tag; props → element properties; events → listeners. */
export type AuToastProps = AuHostProps & Omit<AuToastShape, 'type'> & {
  onAuClose?: AuEventHandler
}
export const AuToast = makeAuWrapper<AuToastProps>('au-toast', ['heading', 'tone', 'dismissible'], { onAuClose: 'au-close' })

/** React wrapper for `<au-toggle-chip>` — renders the tag; props → element properties; events → listeners. */
export type AuToggleChipProps = AuHostProps & Omit<AuToggleChipShape, 'type'> & {
  onAuToggle?: AuEventHandler
}
export const AuToggleChip = makeAuWrapper<AuToggleChipProps>('au-toggle-chip', ['label', 'tone', 'count', 'pressed', 'disabled'], { onAuToggle: 'au-toggle' })

/** React wrapper for `<au-toolbar>` — renders the tag; props → element properties; events → listeners. */
export type AuToolbarProps = AuHostProps & Omit<AuToolbarShape, 'type'>
export const AuToolbar = makeAuWrapper<AuToolbarProps>('au-toolbar', ['size', 'variant', 'overflow', 'disabled'], {})

/** React wrapper for `<au-toolbar-group>` — renders the tag; props → element properties; events → listeners. */
export type AuToolbarGroupProps = AuHostProps & Omit<AuToolbarGroupShape, 'type'>
export const AuToolbarGroup = makeAuWrapper<AuToolbarGroupProps>('au-toolbar-group', ['gap'], {})

/** React wrapper for `<au-toolbar-spacer>` — renders the tag; props → element properties; events → listeners. */
export type AuToolbarSpacerProps = AuHostProps & Omit<AuToolbarSpacerShape, 'type'>
export const AuToolbarSpacer = makeAuWrapper<AuToolbarSpacerProps>('au-toolbar-spacer', [], {})

/** React wrapper for `<au-tooltip>` — renders the tag; props → element properties; events → listeners. */
export type AuTooltipProps = AuHostProps & Omit<AuTooltipShape, 'type'>
export const AuTooltip = makeAuWrapper<AuTooltipProps>('au-tooltip', ['side', 'arrow'], {})

/** React wrapper for `<au-tree>` — renders the tag; props → element properties; events → listeners. */
export type AuTreeProps = AuHostProps & Omit<AuTreeShape, 'type'> & {
  onAuSelect?: AuEventHandler
}
export const AuTree = makeAuWrapper<AuTreeProps>('au-tree', [], { onAuSelect: 'au-select' })

/** React wrapper for `<au-tree-item>` — renders the tag; props → element properties; events → listeners. */
export type AuTreeItemProps = AuHostProps & Omit<AuTreeItemShape, 'type'>
export const AuTreeItem = makeAuWrapper<AuTreeItemProps>('au-tree-item', ['label', 'selected', 'expanded'], {})

/** React wrapper for `<au-tree-row>` — renders the tag; props → element properties; events → listeners. */
export type AuTreeRowProps = AuHostProps & Omit<AuTreeRowShape, 'type'> & {
  onAuActivate?: AuEventHandler
  onAuToggle?: AuEventHandler
}
export const AuTreeRow = makeAuWrapper<AuTreeRowProps>('au-tree-row', ['kind', 'level', 'open', 'selected', 'dirty', 'disabled'], { onAuActivate: 'au-activate', onAuToggle: 'au-toggle' })

/** React wrapper for `<au-typed-value-editor>` — renders the tag; props → element properties; events → listeners. */
export type AuTypedValueEditorProps = AuHostProps & Omit<AuTypedValueEditorShape, 'type'> & {
  onAuValueChange?: AuEventHandler
  onAuValueComplete?: AuEventHandler
}
export const AuTypedValueEditor = makeAuWrapper<AuTypedValueEditorProps>('au-typed-value-editor', ['schema', 'value', 'diagnostics', 'resolveOptions', 'submitLabel'], { onAuValueChange: 'au-value-change', onAuValueComplete: 'au-value-complete' })

/** React wrapper for `<au-viewer-switch>` — renders the tag; props → element properties; events → listeners. */
export type AuViewerSwitchProps = AuHostProps & Omit<AuViewerSwitchShape, 'type'> & {
  onAuActivate?: AuEventHandler
  onAuMore?: AuEventHandler
}
export const AuViewerSwitch = makeAuWrapper<AuViewerSwitchProps>('au-viewer-switch', ['primaryLabel', 'hasMore', 'disabled'], { onAuActivate: 'au-activate', onAuMore: 'au-more' })

/** React wrapper for `<au-workspace-mark>` — renders the tag; props → element properties; events → listeners. */
export type AuWorkspaceMarkProps = AuHostProps & Omit<AuWorkspaceMarkShape, 'type'>
export const AuWorkspaceMark = makeAuWrapper<AuWorkspaceMarkProps>('au-workspace-mark', ['name'], {})

/** React wrapper for `<au-workspace-switcher>` — renders the tag; props → element properties; events → listeners. */
export type AuWorkspaceSwitcherProps = AuHostProps & Omit<AuWorkspaceSwitcherShape, 'type'> & {
  onAuSelect?: AuEventHandler
  onAuOpenFolder?: AuEventHandler
  onAuNewWorkspace?: AuEventHandler
}
export const AuWorkspaceSwitcher = makeAuWrapper<AuWorkspaceSwitcherProps>('au-workspace-switcher', ['name', 'items', 'currentId'], { onAuSelect: 'au-select', onAuOpenFolder: 'au-open-folder', onAuNewWorkspace: 'au-new-workspace' })
