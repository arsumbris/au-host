# Schema-12 repo identity + deps (au.engine.repo).
type: au.engine.repo::au-engine
name: file-tree
description: first-party file-tree projection
deps:
  - name: au-host-sdk
  # `file-tree` declares `handles-intent-meta::intent` in a TYPE position — see the editor's note.
  - name: intent
