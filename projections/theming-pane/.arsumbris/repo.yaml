# Schema-12 repo identity + deps (au.engine.repo).
type: au.engine.repo::au-engine
name: theming-pane
description: first-party theming-pane projection; discovers --au-* tokens via
  the CSSOM and edits them live
deps:
  - name: au-host-sdk
