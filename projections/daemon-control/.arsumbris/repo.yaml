# Schema-12 repo identity + deps (au.engine.repo).
type: au.engine.repo::au-engine
name: daemon-control
description: first-party daemon-control projection
deps:
  - name: au-host-sdk
  # daemon-control fires open-intent / open-pane-intent, so its type-def declares
  # fires-intent-meta::intent and the fired intent def-refs cross into the intent repo.
  - name: intent
