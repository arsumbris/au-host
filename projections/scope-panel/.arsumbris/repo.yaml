# Schema-12 repo identity + deps (au.engine.repo).
type: au.engine.repo::au-engine
name: scope-panel
description: first-party scope-panel projection; views + edits each member's
  .auignore file scope over the ignores/set_ignores surface
deps:
  - name: au-host-sdk
